@model FamilyHubs.SharedKernel.Razor.Errors.Error?

@if (Model != null)
{
    @* todo: need id for aria-describedby on input. create from error? *@
    <p class="govuk-error-message" id="@(Model.InputErrorMessageParaId)">
        <span class="govuk-visually-hidden">Error:</span> @Model.ErrorMessage
    </p>
}